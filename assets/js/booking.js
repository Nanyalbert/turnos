const S=window.BlackTurnosStore;const $=s=>document.querySelector(s);let chosen={date:null,time:null};let calendarCursor=new Date();calendarCursor.setDate(1);
function fmt(date){return new Intl.DateTimeFormat('es-AR',{weekday:'long',day:'numeric',month:'long'}).format(new Date(date+'T12:00:00'))}
function setStep(n){['bookingStep1','bookingStep2','bookingStep3'].forEach((id,i)=>$('#'+id).hidden=i!==n-1);document.querySelectorAll('#bookingProgress span').forEach((el,i)=>el.classList.toggle('active',i<n))}
function loadPros(){const data=S.load();$('#bookingProfessional').innerHTML=data.professionals.filter(p=>p.active).map(p=>`<option value="${p.id}">${p.specialty}</option>`).join('');calendarCursor=new Date();calendarCursor.setDate(1);chosen={date:null,time:null};renderCalendar();setStep(1)}
function monthTitle(){return new Intl.DateTimeFormat('es-AR',{month:'long',year:'numeric'}).format(calendarCursor)}
function startGridDate(){const first=new Date(calendarCursor.getFullYear(),calendarCursor.getMonth(),1);const dow=first.getDay();const offset=dow===0?6:dow-1;const start=new Date(first);start.setDate(first.getDate()-offset);return start}
function renderCalendar(){const box=$('#monthlyCalendar'),proId=$('#bookingProfessional').value;$('#calendarMonthTitle').textContent=monthTitle();box.innerHTML='';const start=startGridDate();const today=new Date();today.setHours(0,0,0,0);for(let i=0;i<42;i++){const d=new Date(start);d.setDate(start.getDate()+i);const date=S.isoLocal(d);const sameMonth=d.getMonth()===calendarCursor.getMonth();const past=d<today;const slots=sameMonth&&!past?S.slotsFor(proId,date):[];const b=document.createElement('button');b.type='button';b.className='calendar-day';b.textContent=d.getDate();if(!sameMonth)b.classList.add('other-month');if(past)b.classList.add('past');if(S.isoLocal(d)===S.isoLocal(today))b.classList.add('today');if(slots.length){b.classList.add('available');b.title=`${slots.length} horario${slots.length===1?'':'s'} disponible${slots.length===1?'':'s'}`;b.addEventListener('click',()=>selectDate(date,b))}else b.disabled=true;if(chosen.date===date)b.classList.add('selected');box.appendChild(b)}const now=new Date();$('#prevMonth').disabled=calendarCursor.getFullYear()===now.getFullYear()&&calendarCursor.getMonth()===now.getMonth()}
function selectDate(date,btn){chosen.date=date;chosen.time=null;document.querySelectorAll('.calendar-day').forEach(x=>x.classList.remove('selected'));btn.classList.add('selected');const slots=S.slotsFor($('#bookingProfessional').value,date);$('#chosenDateLabel').textContent=`${fmt(date)}. Elegí uno de los horarios disponibles.`;$('#availableSlots').innerHTML=slots.length?slots.map(t=>`<button type="button" class="slot-btn" data-time="${t}">${t}</button>`).join(''):'<p class="hint">No quedan horarios disponibles para este día.</p>';document.querySelectorAll('.slot-btn').forEach(b=>b.addEventListener('click',()=>selectTime(b.dataset.time)));setStep(2)}
function selectTime(time){chosen.time=time;const data=S.load(),p=data.professionals.find(x=>x.id===$('#bookingProfessional').value);$('#selectedSummary').innerHTML=`<strong>${p.specialty}</strong><span>${fmt(chosen.date)} · ${time}</span>`;setStep(3)}
$('#bookingProfessional').addEventListener('change',()=>{chosen={date:null,time:null};calendarCursor=new Date();calendarCursor.setDate(1);renderCalendar();setStep(1)});$('#prevMonth').addEventListener('click',()=>{calendarCursor.setMonth(calendarCursor.getMonth()-1);renderCalendar()});$('#nextMonth').addEventListener('click',()=>{calendarCursor.setMonth(calendarCursor.getMonth()+1);renderCalendar()});$('#backToDates').addEventListener('click',()=>setStep(1));$('#backToSlots').addEventListener('click',()=>setStep(2));
$('#publicBookingForm').onsubmit=e=>{e.preventDefault();const f=Object.fromEntries(new FormData(e.target)),proId=$('#bookingProfessional').value,data=S.load(),p=data.professionals.find(x=>x.id===proId);try{S.createAppointment({professionalId:proId,date:chosen.date,time:chosen.time,duration:p.slotDuration,patientName:f.patientName,phone:f.phone,email:f.email||'',service:p.specialty,notes:f.notes||'',source:'web',status:'pending'});$('#bookingStep3').hidden=true;$('#bookingSuccess').hidden=false;document.querySelectorAll('#bookingProgress span').forEach(x=>x.classList.add('active'));$('#successSummary').textContent=`${fmt(chosen.date)} a las ${chosen.time} · ${p.specialty}`;}catch(err){alert(err.message);setStep(1);renderCalendar()}};
loadPros();